version: '3'
services:
  airflow-scheduler:
    environment:
      - AIRFLOW__CORE__LOGGING_LEVEL=DEBUG

  airflow-webserver:
    environment:
      - AIRFLOW__CORE__LOGGING_LEVEL=DEBUG

  airflow-worker:
    environment:
      - AIRFLOW__CORE__LOGGING_LEVEL=DEBUG
